export interface IExpenditures {
  description: string;
  date: string;
  category: string;
  amount: string;
}

export interface IMeeting {
  minutes: string;
  date: string;
  type: string;
  zoom: string;
}

export interface IDocs {
  description: string;
  link: string;
}
